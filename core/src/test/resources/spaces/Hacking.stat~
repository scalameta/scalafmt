maxColumn = 100
<<< spaces in paretheses
function(a, b, c)
>>>
function( a, b, c )
<<< spaces in sbt
lazy val test = (project in file("test"))
  .settings(
    // Name
    name := "test",

    // Version
    version := "1.0.0",

    // Blah
    scalaVersion := "2.11.8"
  )
>>>
lazy val test = (project in file("test"))
  .settings(
    // Name
    name := "test",

    // Version
    version := "1.0.0",

    // Blah
    scalaVersion := "2.11.8"
  )
