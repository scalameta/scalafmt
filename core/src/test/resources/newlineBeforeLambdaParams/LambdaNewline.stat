40 columns                              |
<<< ONLY newline before lambda params {
lst.map { x =>
      println(x)
  x + 1
}
>>>
lst.map {
  x =>
    println(x)
    x + 1
}
<<< ONLY don't apply to one-liners
lst.map { x => x }
>>>
lst.map { x => x }
<<< ONLY but preserve newlines if inserted
lst.map {
  x =>
    x
}
>>>
lst.map {
  x =>
    x
}
