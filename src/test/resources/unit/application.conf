// Typesafe config could be an overkill for this simple stuff,
// but I suspect I will want to add more config later.
speed {
  hostname = "speed.scalafmt.org"
  port = 443
  https = true
  username = ${?SCALAFMT_USERNAME}
  password = ${?SCALAFMT_PASSWORD}
}
